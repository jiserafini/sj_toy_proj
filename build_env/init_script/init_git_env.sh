#!/bin/bash

git config --global user.name "Sangjung Woo"
git config --global user.email again4you@gmail.com
git config --global color.ui "auto"
git config --global core.editor "vim"

git config --global --list
